<UserControl x:Class="ServerStartupSuite.UserManagementPane"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900"
             Background="White">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,20">
            <TextBlock Text="User &amp; Group Management"
                       FontSize="36"
                       FontWeight="ExtraBold"
                       Foreground="#891218"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,15"/>

            <TextBlock Text="Create User or Group" Foreground="#891218" FontWeight="Bold" HorizontalAlignment="Center" FontSize="22" Margin="0,0,0,5"/>
            <ComboBox x:Name="ComboMode" MinWidth="280" Width="Auto" Height="35" VerticalContentAlignment="Center" SelectionChanged="ComboMode_SelectionChanged">
                <ComboBoxItem Content="Add New User" IsSelected="True"/>
                <ComboBoxItem Content="Create User Group"/>
            </ComboBox>
        </StackPanel>

        <Border Grid.Row="1" Background="#F4F4F4" Padding="10" HorizontalAlignment="Center" Width="400" Margin="0,0,0,20">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock Text="Domain: " Foreground="#891218" FontWeight="Bold" FontSize="18"/>
                <TextBlock x:Name="TxtEnvStatus" Text="Identifying Domain" Foreground="Black" FontSize="18"/>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="2" VerticalAlignment="Top" HorizontalAlignment="Center">
            <StackPanel Margin="0,10">
                <TextBlock Text="Username/GroupName" Foreground="#891218" FontWeight="Bold" FontSize="22" Margin="0,0,0,5"/>
                <TextBox x:Name="TxtTargetName" Width="400" Height="50" VerticalContentAlignment="Center" BorderBrush="Black" Padding="5,0"/>
            </StackPanel>

            <StackPanel x:Name="PasswordPanel" Margin="0,10">
                <TextBlock Text="Password" Foreground="#891218" FontWeight="Bold" FontSize="22" Margin="0,0,0,5"/>
                <PasswordBox x:Name="TxtPassword" Width="400" Height="50" VerticalContentAlignment="Center" BorderBrush="Black" Padding="5,0"/>
            </StackPanel>
        </StackPanel>

        <StackPanel Grid.Row="3" HorizontalAlignment="Center">
            <Button x:Name="BtnExecute" Content="Create" FontSize="25" 
                    Style="{StaticResource NmsuButtonStyle}" 
                    Width="280" Height="45" Margin="0,0,0,15" 
                    Click="BtnExecute_Click"/>

            <Button x:Name="BtnBack" Content="Exit" FontSize="25" 
                    Style="{StaticResource NmsuButtonStyle}" 
                    Width="280" Height="45" 
                    Click="BtnBack_Click"/>
        </StackPanel>
    </Grid>
</UserControl>